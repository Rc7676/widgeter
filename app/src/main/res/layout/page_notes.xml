<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/page_notes_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="96dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="14dp">

            <!-- Gradient hero header -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_hero"
                android:orientation="vertical"
                android:padding="20dp">

                <TextView
                    android:id="@+id/hero_greeting"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Hello 👋"
                    android:fontFamily="@font/sora_family"
                    android:letterSpacing="-0.01"
                    android:textColor="?attr/heroOn"
                    android:textSize="24sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/hero_date"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:alpha="0.85"
                    android:textColor="?attr/heroOn"
                    android:textSize="13sp" />

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="18dp"
                    android:baselineAligned="false"
                    android:orientation="horizontal">

                    <LinearLayout style="@style/StatTile">
                        <TextView android:id="@+id/stat_notes" style="@style/StatNumber" />
                        <TextView style="@style/StatLabel" android:text="@string/stat_notes" />
                    </LinearLayout>

                    <LinearLayout style="@style/StatTile">
                        <TextView android:id="@+id/stat_counters" style="@style/StatNumber" />
                        <TextView style="@style/StatLabel" android:text="@string/stat_counters" />
                    </LinearLayout>

                    <LinearLayout style="@style/StatTile">
                        <TextView android:id="@+id/stat_tasks" style="@style/StatNumber" />
                        <TextView style="@style/StatLabel" android:text="@string/stat_tasks" />
                    </LinearLayout>

                    <LinearLayout style="@style/StatTile">
                        <TextView android:id="@+id/stat_streak" style="@style/StatNumber" />
                        <TextView style="@style/StatLabel" android:text="@string/stat_streak" />
                    </LinearLayout>
                </LinearLayout>
            </LinearLayout>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:layout_marginStart="6dp"
                android:layout_marginBottom="2dp"
                android:text="@string/section_note"
                android:fontFamily="@font/manrope_family"
                android:textAllCaps="true"
                android:letterSpacing="0.14"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textSize="11sp"
                android:textStyle="bold" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/notes_recycler"
                    android:layoutAnimation="@anim/layout_slide_up"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:nestedScrollingEnabled="false" />

            <LinearLayout
                android:id="@+id/notes_empty"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:paddingTop="24dp"
                android:paddingBottom="12dp"
                android:paddingHorizontal="32dp">
                <TextView style="@style/EmptyIcon" android:text="📝" />
                <TextView style="@style/EmptyTitle" android:text="@string/notes_empty_title" />
                <TextView style="@style/EmptySub" android:text="@string/notes_empty_sub" />
            </LinearLayout>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/notes_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:text="@string/new_note"
        app:icon="@drawable/ic_add" />

</FrameLayout>

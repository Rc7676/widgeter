<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/page_counters_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:visibility="gone">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout style="@style/PageHeader">
            <LinearLayout
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:orientation="vertical">
                <TextView style="@style/PageHeaderTitle" android:text="@string/section_counter" />
                <TextView style="@style/PageHeaderSub" android:text="@string/counters_header_sub" />
            </LinearLayout>
            <TextView
                android:id="@+id/counters_header_count"
                style="@style/HeaderBadge"
                android:text="0" />
        </LinearLayout>

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/counters_recycler"
                    android:layoutAnimation="@anim/layout_slide_up"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingHorizontal="12dp"
                android:paddingTop="4dp"
                android:paddingBottom="104dp" />

            <LinearLayout
                android:id="@+id/counters_empty"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:orientation="vertical"
                android:gravity="center"
                android:paddingBottom="48dp"
                android:paddingHorizontal="32dp">
                <TextView style="@style/EmptyIcon" android:text="🔢" />
                <TextView style="@style/EmptyTitle" android:text="@string/counters_empty_title" />
                <TextView style="@style/EmptySub" android:text="@string/counters_empty_sub" />
            </LinearLayout>
        </FrameLayout>
    </LinearLayout>

    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/counters_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:text="@string/new_counter"
        app:icon="@drawable/ic_add" />

</FrameLayout>
